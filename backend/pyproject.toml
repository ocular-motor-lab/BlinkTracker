[project]
name = "blink-tracker-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "opencv-python>=4.10",
  "pandas>=2.2",
  "mediapipe>=0.10"
]

[tool.pytest.ini_options]
pythonpath = ["app"]
testpaths = ["app/tests"]

